[project]
name = "mes-bot"
version = "1.0.0"
description = "MES-система производства стеклопластиковых труб: Telegram Bot + Mini App (aiogram 3, FastAPI, PostgreSQL)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
